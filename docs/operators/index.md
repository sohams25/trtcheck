# Operators

Per-operator TensorRT support tables are generated from
`trtcheck/data/operator_matrix.json`. The generator (added in a
follow-up step) writes one page per operator into `docs/operators/`.

Until the generator lands, this section is empty.
